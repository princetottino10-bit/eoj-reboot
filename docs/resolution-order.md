# 盤面の処理順序と多対象攻撃の裁定

Status: 確定(§6 のみ未決)
Date: 2026-07-29
関連: [`effect-rank-design.md`](effect-rank-design.md) §6 / `current-spec-ver1.md` §2

---

## 1. マスの番号と記法(確定)

盤の9マスに、**左上から右下へ 1〜9** を振る。

```
 1  2  3          空   陰   空
 4  5  6    =     陰  太極  陽
 7  8  9          空   陽   空
```

番号は**マスに固定**する。駒ではないので、召喚・撃破・移動が起きても番号は変わらない。

### 1.1 覚え方(設計上の副産物)

| | |
|---|---|
| **偶数マス(2・4・6・8)** | すべて属性マス(陰・陽) |
| **奇数マス(1・3・5・7・9)** | すべて空マス。うち5が太極 |

**太極が5**になるのは、風水の**九宮(洛書)**の魔方陣の中央が5であることと一致する。盤面に数字を刷ることが、世界観の側からも自然に説明できる。

### 1.2 盤の向きは固定する(重要)

**対面2人プレイなので「左上」はプレイヤーごとに異なる。** 片方から見た1は、もう片方から見れば9になる。

- **番号は盤面に印刷し、両者が同じ向きで読む**こと
- デジタル版も同じ絶対番号を使うこと。プレイヤーごとに回転させない

### 1.3 攻撃範囲の座標とは別物

正本の攻撃範囲は `[-1,1]` `[0,1]` のような**相対座標**で、第2成分が前後(+1が前方)、第1成分が左右。これは**式神の向きに依存する**。

一方このマス番号は**盤に固定された絶対座標**。両方が必要で、混同しないこと。

- マス番号 n → 行 = `⌈n/3⌉`、列 = `((n−1) mod 3) + 1`

---

### 1.4 攻撃範囲・死角の記法(2026-07-29 確定)

**向きを上に固定し、周囲のマスに盤と同じ読み順で番号を振る。** 盤面の 1〜9(§1)と同じ「左上から」なので、覚える規則は1つで足りる。

```
    0        ← 前2(直線2マス先。ここだけ距離2)
 1  2  3
 4  ●  6     ● = その式神自身
 7  8  9
```

書式は **`範囲 / 死角`**。範囲・死角とも、該当するマス番号を昇順で並べる。

| | |
|---|---|
| `2/8` | 前1が範囲、後1が死角 |
| `123/8` | 前3マスが範囲、後1が死角 |
| `02/46` | 前1と前2が範囲、左右が死角 |

#### 対象数は「全」の1文字だけ

**無印 = 範囲内から1体を選ぶ。`全` = 範囲内の全てを攻撃(敵味方問わず・強制)。**

```
2/8        単体（1マスなので選ぶ余地がない）
123/8      範囲から選択（3マスのうち1体）
123全/8    範囲全体（3マス全部・味方も巻き込む）
```

**`単` は書かない。範囲が1桁なら選択の余地がなく、記法から自明だから。** 印が付くのは「味方も巻き込む」という事故る側だけ ── **驚くほうのルールにだけ印が付く**形にしてある。逆にすると無印カードを見るたびに全体かどうかの確認が要る。

将来「範囲内から2体まで」が必要になったら `123:2/8` のように数字を足して拡張できる。現時点では不要。

#### 素体と並べて1行で書く

```
薙刀霊   4/5/2 · 123/8
九尾     6/7/3 · 12346/8
龍       7/8/4 · 0246/8
```

前半が `召喚霊力/HP/ATK`、後半が `範囲/死角`。

#### この記法から形状予算が直読みできる

[`effect-rank-design.md`](effect-rank-design.md) §7 の形状係数は、記法の見た目からそのまま出る。

| 記法の特徴 | 形状予算 |
|---|---|
| **範囲の桁数** | マス数。加点段(3桁 = 3マス = +1.5) |
| **`0` を含むか** | 非隣接ボーナス +1.0 の有無 |
| **死角の桁数** | 減点(2桁 = −1.5) |

`0246` を見た瞬間に「4マス + 非隣接 = +3.0」と出る。検算表を引く必要がない。

---

## 2. 基本原則(確定)

> **同時に複数の処理が発生する場合、マス番号の若い順に処理する。**

多対象攻撃だけでなく、盤面上の複数の駒・マスに同時に働く効果すべてに適用する(術式「大地の怒り」など)。

---

## 3. 多対象攻撃の解決手順(確定・一括解決)

RULE_SPEC §10 の単体攻撃の順序 `攻撃宣言 → 補正 → ダメージ計算 → 撃破判定 → 反撃 → 最終撃破判定` を、**構造を保ったまま**多対象へ拡張する。

```
1. 攻撃宣言 ── 対象をすべて決定する
2. 補正 ──── 対象ごとに死角を個別判定            (若い順)
3. ダメージ ── 全対象に同時に適用
4. 撃破判定 ── 全対象をまとめて判定
5. 反撃 ──── 生き残った対象のうち、
              反撃範囲に攻撃側がいるものが反撃    (若い順)
6. 最終撃破判定 ── 攻撃側
```

### 3.1 逐次解決を採らない理由

対象1を殴る → 反撃 → 対象2を殴る → 反撃、という順にすると:

- 撃破判定と反撃が交互に来て、**§10 の順序構造が壊れる**
- 1体目の反撃で攻撃側が撃破されると、**2体目への攻撃が消える**
- 卓上で盤面を何度も行き来する

一括なら「撃破された相手は反撃しない」という既存ルールが全対象に同じく効き、処理も1往復で済む。

### 3.2 帰結 — 2体とも落とせば反撃はゼロ

一括解決では、ダメージ適用が撃破判定より先なので、**両方を撃破できれば反撃を1回も受けない**。逐次解決に比べて攻撃側に有利。これは対象数の値付けに直接効く(§5)。

---

## 4. 死角は対象ごとに個別判定(確定)

1回の攻撃の中で、**ある対象は死角から(ATK+2・反撃なし)、別の対象は正面から(反撃あり)**、という状態が普通に発生する。これを認める。

> **補正は死角だけ。属性は攻撃補正ではない。** 属性は `ATTRIBUTE_HP_BONUS=2`(属性マスに乗っている式神のHP+2)であって、攻撃時に乗る値ではない。RULE_SPEC §10 の補正ステップからは 2026-07-04 に削除済み。

### 帰結

1. **「片方だけ死角を取る」置き方が最適解になる。** 2体とも死角を取れる配置(相手2体が同時に背中を向けている)は稀なので、実戦では常に「どちらを死角から殴るか」の選択になる。**向きの読み合いが濃くなる方向**で、ゲームの核心に効く
2. **反撃も個別。** 同じ1回の攻撃で、反撃する対象としない対象が混ざる。**卓で必ず質問が出るので、ルールブックに明記すること**
3. **上限は ATK+2 が2体分。** ATK2の駒が両方死角なら 4+4=8ダメージで、C4(HP5)を2体まとめて落とせる。発生は稀だが上限として認識しておく

---

## 5. 属性補正と移動時の再計算(2026-07-29 確定)

### 5.1 属性補正の基本

| マス | 補正 |
|---|---:|
| その式神と**同じ属性**のマス | **HP +2** |
| **反対属性**のマス | **HP −2** |
| 空マス・太極 | ±0 |

`ATTRIBUTE_HP_BONUS = 2`。**太極は空マスと同じ扱い**(2026-07-18 確定「太極はマナ−1でHP補正なし」)なので、移動先が空か太極かを区別する必要はない。

原作でも「自属性のフィールドで +2HP、反対属性で −2HP、中立は増減なし」で同じ。

### 5.2 移動したら遡って再計算する(案A)

> **移動先のマスの属性で最大HPを引き直し、それまでに受けたダメージを新しい最大HPから引く。0以下になったらその式神は撃破される。**

移動で死ぬ。これを許容する。

例: 鬼火(素の HP2)が陰マスに乗って最大4。ダメージ3を受けて現在1。ここから空マスへ手繰られると ──

```
最大HP  4 → 2
現在HP  2 − 3(既に受けたダメージ) = −1  →  撃破
```

**採らなかった案B**(最大HPだけ引き直し、現在HPは上限で切り下げる。移動では死なない)との違いは、**傷ついた駒の扱いが正反対**になること。Aは傷ついた駒ほど死に、Bは傷ついた駒ほど損しない。

### 5.3 帰結 — 移動系の術式が「条件付き除去」になる

これは方針の変更にあたるので明記しておく。`spell-candidates.md` §5 では**除去を除外**している(撃破決着を増やす方向のため)が、案Aを採ると移動系がその役割を部分的に持つ。

| 術式 | 変化 |
|---|---|
| **手繰り**(敵1体を移動) | **傷ついた敵を属性の合わないマスへ動かして撃破できる。** 実質的に条件付き除去。ランクは上がる |
| **逆手**(自分と敵を入れ替え) | 再計算が2件走る。**自分の駒も死にうる** |
| **窮鼠**(自分1体を任意の空きマスへ) | 傷ついた駒を動かすと死にうる。**ただし自属性マスへ逃がせば +2 回復する**(§5.4.1)ので両刃 |
| 地脈(空きマスの属性を変更) | 空きマス限定なので影響なし。この安全装置は正しかった |

無条件除去とは別物(傷んでいること + 属性が変わる移動先があること、の2条件が要る)だが、**撃破決着率を押し上げる方向**であることは記録しておく。§4.1 の配分原則(守備側に厚く配る)と逆行するので、効果側で補正が要るかもしれない。

### 5.4 卓上の処理は「トークンを差分だけずらす」だけ

「遡って再計算」と書くと大仰だが、**現在HPをトラック上のトークンで管理している現行方式のまま、追加の記録なしで処理できる**。

```
新しい現在HP = 今の現在HP + (移動先の補正 − 移動元の補正)
```

| 移動 | トークンの動き |
|---|---|
| 自属性 → 空/太極 | **2つ下げる** |
| 自属性 → 反対属性 | **4つ下げる** |
| 空/太極 → 自属性 | **2つ上げる** |
| 反対属性 → 自属性 | **4つ上げる** |

0以下になったらその式神は撃破される。受けたダメージ量を別に覚えておく必要はない。

### 5.4.1 移動は回復にもなる

差分方式で見ると当たり前だが、**傷ついた駒を自属性のマスへ動かせば HP が戻る**。

これは §5.3 の見立てを一部打ち消す。窮鼠(自分1体を任意の空きマスへ)は「傷んだ駒を動かすと死ぬ」一方通行ではなく、**傷んだ駒を自属性マスへ逃がして +2 回復させる**使い方を持つ。**カムバック機構としてはむしろ噛み合う。**

手繰り(敵1体を移動)も同様に両刃で、敵を反対属性へ押し込めば撃破しうるが、置き場所を間違えると敵を回復させる。

---

## 6. 対象数の値付けへの影響

詳細は [`effect-rank-design.md`](effect-rank-design.md) §6。

**結論だけ**: 一括解決(§3.2)は対象数を強くする方向に働くが、**対象は「範囲内全体を強制」で確定した**(2026-07-29)ため、盤面が埋まるほど自分の駒を巻き込むようになる(禁止されるのではなく、巻き込んだうえで撃つことは選べる)。勝ちに近づくほど使いづらくなるので、差し引き **効果ランク1前後**(範囲3マスの場合。範囲の広さで変わる ── `effect-rank-design.md` §6.4)。

『対象数』のカード表記は `単体`(範囲1マス) / `範囲から選択`(範囲複数マスから1体) / `範囲全体`(強制・FFあり) の3つだが、**前2つは同じルールなので、値付けの軸としては 選択 / 全体 の2値**(ランク 0 / 1・仮)。

本書 §3〜§4 の一括解決と死角の個別判定は3表記すべてに適用し、**§6.6 の味方関連の裁定は `範囲全体` にのみ適用する**。

---

## 7. 未決事項

### 7.1 術式「相打ち」の扱い(保留)

`jutsushiki-deck-patterns.md` の「相打ち」(互いの射程内にいる敵2体に互いを攻撃させる)は、§2 の若い順を素直に適用すると、番号の小さい側が先に攻撃し、**相手を倒したらもう片方は攻撃できない**。「相打ち」という名前に対して片方だけ死ぬケースが多発する。

§3 の一括解決(ダメージ同時適用)を適用すれば名前と挙動が揃うが、**このカード自体の採否がまだ決まっていない**ため保留。採用を決める段階で合わせて裁定する。

### 7.2 その他

- [ ] 対象数を素体パラメータ(設計シートの `対象数` 列)として扱うか、効果ランクとして扱うか。現状シートは素体列、方針は効果扱いで**食い違っている**
- [ ] 盤面への番号印刷。印刷キットとゲームボードのデータに反映が必要
- [ ] エンジン側のマス番号↔内部座標の対応定義
- [ ] 移動系術式(手繰り・逆手・窮鼠)のランクを見直す。撃破にも回復にも働く両刃になった(§5.3、§5.4.1)
- [ ] ルールブックへの反映(特に §4 の死角個別判定と §5.2 の再計算)
